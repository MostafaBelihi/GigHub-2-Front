import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
	// + This component is the carrier of the routed component that will appear in index.html via its selector.
	// + Check its template.
}
